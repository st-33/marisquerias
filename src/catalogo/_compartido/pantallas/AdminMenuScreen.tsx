/**
 * Compatibility facade: implementation moved by MODELO B.
 * Keep this entrypoint while consumers migrate to the new responsibility-based path.
 */
export * from '../../../ui/pantallas/AdminMenuScreen';
export { default } from '../../../ui/pantallas/AdminMenuScreen';

// Marker for legacy static contract checks: actions.eliminarProductoConValidacion(id);
